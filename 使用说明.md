# Telegram MCP 使用说明

让 Cursor 用你的 Telegram 账号**查消息、搜聊天、看联系人**。当前是只读：不能代发、不能删、不能改资料。

上游项目：<https://github.com/chigwell/telegram-mcp>（英文 README 是完整技术文档）。

## 现状（2026-08-25）

| 项 | 状态 |
|---|---|
| 登录 | 已从本机 Telegram 桌面版导入，无需再去 my.telegram.org 建应用 |
| 权限 | `TELEGRAM_EXPOSED_TOOLS=read-only`，51 个只读工具 |
| Cursor | 项目配置 `.cursor/mcp.json`，服务器名 `telegram-mcp` |
| 代理 | Clash Verge `127.0.0.1:7897`，**必须开着**，否则 Cursor 里会报错 |

敏感信息在 `.env` 和 `*.session`，已 gitignore，不要提交、不要发给别人。

## 日常怎么用

Clash 开着，Cursor 里 `telegram-mcp` 绿灯。直接用中文说，例如：

- 最近有哪些未读？
- 搜「AI」相关聊天
- 打开「每日AI资讯」最近 20 条
- 这个群有哪些管理员？

## 能做什么

**对话列表：** 会话、群、频道；可筛未读/免打扰/已归档。

**读消息：** 历史、搜索（单个聊天或全局）、上下文、置顶、定时发送、反应、已读情况、消息链接、内联按钮。

**联系人：** 列表、搜索、导出、黑名单、和某人的私聊/共同群、上次互动。

**群/频道：** 成员、管理员、被封用户、邀请链接、管理日志、话题。

**资料：** 自己的账号、别人资料/头像/在线状态、隐私设置、Bot 信息。

**媒体信息：** 消息里的媒体元数据、贴纸包、GIF 搜索（只看，不发）。

**文件夹与草稿：** 列表、某个文件夹详情、未发送草稿。

**等新消息：** `wait_for_new_message` / `wait_for_settled_message`（会卡住等回复）；`incoming_feed_status` 看回调状态。

## 不能做什么

发消息、回复、转发、删除、置顶操作、改群、改资料、下载文件到磁盘——这些写入工具都没打开。

若要开放发送，把 `.env` 里的 `TELEGRAM_EXPOSED_TOOLS` 改成 `all` 或 `read-only+send_message,reply_to_message`，然后重启 MCP。

## 出问题先看

1. Clash Verge 是否在跑（端口 7897）。
2. Cursor Settings → Tools & MCP 里是否只有 **一条** `telegram-mcp` 绿灯。两条会抢同一把登录钥匙，过一会儿会互相踢掉。
3. 不要同时让两个 Cursor 窗口各开一个 MCP 去连同一个 session。

重新登录（很少需要）：关掉 Telegram 桌面版后，用桌面版 tdata 再生成 session。`my.telegram.org` 创建应用在国内常会只弹 `ERROR`，不必再走那条路。
